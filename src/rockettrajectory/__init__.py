import numpy as np
def launch_angle_range(ve_v0, alpha, tol_alpha)
    while 
    phi = np.arcsin((1+alpha)*np.sqrt(1-(alpha/(1+alpha))*(ve_v0)**2)) 
    return phi_range
def main()
    
if __name__ == '__main__': 
    main()
